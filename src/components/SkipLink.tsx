/**
 * Phase 17 — keyboard users get past the manifest bar and the nav in one tab.
 * Visually hidden until focused.
 */
export default function SkipLink() {
  return (
    <a href="#main" className="skip-link">
      SKIP TO CONTENT
    </a>
  );
}
